import dominio.Autor;
import dominio.Copia;
import dominio.Lector;
import dominio.Libro;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class TestGeneral {
        /*private Libro libroRandom = new Libro();
        private Copia copia1 = new Copia();
        private Autor autor1 = new Autor();
        private Lector lector1 = new Lector();

        @BeforeEach
        public void init() {
            libroRandom.getCopias().add(copia1);
        }

        @Test
        @DisplayName("Un lector puede pedir un prestamo")
        public void unLectorPuedePedirUnPrestamo() {
            Assertions.assertTrue();
            Assertions.assertEquals();
         */

}
